---

deployments:
  - modules:
      - path: aws-icc
        environments:
          dev0: true
          dev1: true
          dev2: true
        options:
          build_steps:
            - npm install
            - npm run build
          build_output: dist
    regions:
      - us-east-1